spring:
  datasource:
    url: jdbc:h2:mem:newshubdb;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console
  
  # Disable real Kafka/Redis autoconfig via exclusion in main class or properties if possible
  # But we will handle it via Profiles and Bean mocking
  
app:
  jwt:
    secret: 5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
    expiration: 86400000
  crawler:
    enabled: true
    interval: 3600000

logging:
  level:
    root: INFO
    com.newshub.backend: DEBUG
